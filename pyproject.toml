[tool.black]
target-version = ["py312"]

[tool.ruff]
target-version = "py312"

[tool.ruff.lint.per-file-ignores]
# test_barcode_roundtrip.py calls pytest.importorskip() before its imports so
# the module skips cleanly when optional barcode deps are absent; the imports
# below it are intentionally not at the top of the file.
"tests/test_barcode_roundtrip.py" = ["E402"]
